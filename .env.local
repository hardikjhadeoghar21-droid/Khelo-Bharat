# Service account credentials for Firebase Admin SDK (server-side)
# Replace with the values from your downloaded service account JSON key.
# Ensure that any newline characters (\n) in the private key are preserved exactly as they are.
FIREBASE_PROJECT_ID="your-project-id"
FIREBASE_CLIENT_EMAIL="your-service-account-email@your-project.iam.gserviceaccount.com"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
